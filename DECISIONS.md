# 长期技术决策

## 2026-09-02：保留原生 Java 与自定义构建链

决定：继续使用原生 Android Java、动态 View UI 和根目录 `build.ps1`，不迁移到 Gradle、Kotlin 或 Jetpack Compose。

原因：当前应用、数据格式、资源打包和原生模块已经围绕这套结构运行；迁移会扩大风险和验证范围。

影响：新功能应优先复用 `MainActivity.java` 的现有渲染、状态和辅助方法；构建依赖本机 JDK、Android SDK、CMake 和 NDK。

## 2026-09-02：本地优先，云同步显式启用

决定：饮食记录和个人设置默认保存在本机；云端能力只在用户登录并主动启用同步时使用。API Key、接口地址和模型配置只保存在本机。

原因：本地记录应在无账号、无网络时继续可用，同时避免把高敏感接口凭据纳入云端数据。

影响：修改同步、备份或资料结构时必须保持本地数据兼容，并明确区分可同步数据和本机专属配置。

## 2026-09-02：按需下载非默认字体

决定：APK 只内置默认字体，其他笔记字体从云端按需下载；下载文件保存在应用私有目录，覆盖更新不应删除或重复下载已存在的字体。

原因：字体体积会显著增大安装包，按需下载可以降低初始 APK 体积。

影响：字体列表、云端 `server/fonts` 目录、下载缓存和字体迁移逻辑必须保持名称兼容；发布时不能清理用户本地已下载字体。

## 2026-09-02：通知使用系统闹钟并在系统重启后恢复

决定：三餐提醒由 `MealReminderReceiver` 配合 `AlarmManager` 和通知渠道实现，并响应设备启动及应用更新广播重新安排提醒。

原因：提醒需要在应用未打开时工作，且应适配 Android 8+ 通知渠道和 Android 13+ 通知权限。

影响：修改通知时要同时检查 Manifest 权限、通知权限、PendingIntent、闹钟取消和设备重启恢复；厂商后台限制仍需实机验证。

## 2026-09-02：版本号严格递增

决定：每个正式版本同时更新 `AndroidManifest.xml` 的 `versionName` 和 `versionCode`，`versionCode` 必须递增；更新清单和发布附件使用同一版本。

原因：Android 覆盖安装和应用内检测更新依赖版本号，历史上曾出现版本显示和更新清单不一致。

影响：发布前必须核对 Manifest、`server/update.json`、`dist/update.json`、APK 元数据、Git 标签和远端附件。
